# AI_RESUME_ANALYZER
A Python tool to extract text from PDF, DOCX, and Images using OCR. It validates files by scanning for key resume sections like "Experience" and "Skills." This ensures only relevant documents are processed, making it a fast and flexible utility for automated resume parsing and HR tech.
